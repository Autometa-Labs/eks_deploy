---
# =============================================
# Ansible Inventory for Local Execution
# All tasks run on localhost since we're managing Kubernetes/AWS resources
# =============================================

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
